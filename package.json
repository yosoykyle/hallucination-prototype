{
  "name": "hallucination-prototype",
  "version": "1.0.0",
  "description": "A browser-based tool to induce, analyze, and visualize AI hallucinations across multiple models simultaneously. No server, no build step — open `index.html` and go.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "echo Open index.html in your browser. No server needed.",
    "postinstall": "echo All dependencies installed. Open index.html to run."
  },
  "dependencies": {
    "animejs": "^3.2.2",
    "dexie": "^4.4.3",
    "morphdom": "^2.7.8"
  }
}
